# arcdps developer guidance log

Running log of implementation guidance relayed from the arcdps developer. Each entry gets a
status and is folded into a milestone task when applicable. Newest last.

| # | Guidance | Status |
|---|----------|--------|
| 1 | Use `CBTS_WVWTEAMS` to configure WvW team ids from the log rather than hardcoding red/blue/green tables. | Done — M2 Task 2b. `CBTS_WVWTEAMS` = sc 74 (verified vs arcdps README + GW2EI). Dynamic primary source; static table (from axibridge wvwTeams.ts) is fallback for older logs. Real-log coverage pending a post-May-2026 capture. |
| 2 | Track `CBTS_STUNBREAK` alongside CC metrics. | Done — M2 Task 3. `CBTS_STUNBREAK` = sc 56. Per-player `cc.stun_breaks` + `cc.removed_stun_duration_ms`; EI adapter places them in `support[0]` (verified vs real dps.report EI JSON — the brief's `defenses[0]` guess was wrong). Fixture-exact: 20 breaks / 16907 ms. |
| 3 | Content-local ids (`n_contentlocal`: EFFECT=0, MARKER=1, SKILL=2, SPECIES_NOT_GADGET=3, TEAM=4, EMOTE=5, TRANSFORMATION=6) are session-local — map to stable GUIDs via `CBTS_IDTOGUID`. | Done — M2 Task 2b. `CBTS_IDTOGUID` = sc 46, decoded into `RawLog.guid_map` (guid = src_agent||dst_agent LE bytes, local id = skillid — layout verified vs GW2EI). TEAM guids on `encounter.teams[].guid`; SKILL/SPECIES retained for M3 buff/skill identity; MARKER consumed by Task 7. |
| 4 | `CBTS_MARKER` provides above-target markers; EI probably has the squad-marker GUIDs in an enum somewhere. | Done — M2 Task 7. `CBTS_MARKER` = sc 37 (hand-counted against the arcdps README + cross-checked against GW2EI's `ArcDPSEnums.StateChange.Marker = 37`). Decoded in `wvw::markers::resolve_markers`, resolving the `value` content-local id to a GUID via `CBTS_IDTOGUID` (MARKER=1) and then to a name via the 8-entry `SQUAD_MARKER_NAMES` table (sourced from GW2EI's `MarkerGUIDs.cs`, `Arrow`/`Circle`/`Heart`/`Square`/`Star`/`Swirl` (not "Spiral")/`Triangle`/`X`), falling back to the raw hex GUID for anything else. Native schema: `Player.marker`/`Enemy.marker`, plus `Encounter.markers[]` (full assignment timeline). Verified against the real WvW fixture (181 raw marker events, 5 real assignments; one resolves to a known GUID, one to an unknown-hex fallback). |
| 5 | `CBTS_MARKER` also yields the commander tag's GUID — use it to show the tag's cat/colour variant. | Done — folded into M2 Task 7. `buff == 1` on a `CBTS_MARKER` event flags it as a commander tag (per the arcdps README; corroborated by GW2EI's `MarkerGUIDEvent.IsCommanderTag`, which cross-checks the same GUID set). The GUID resolves via an 18-entry `COMMANDER_TAG_VARIANTS` table (9 colours × standard/"catmander", also from GW2EI's `MarkerGUIDs.cs`) to a `"<color>-commander"`/`"<color>-catmander"` variant string, falling back to the raw hex GUID for an unrecognized tag colour. Native schema: `Player.commander_tag { variant, guid }`, alongside the (now correctly populated — previously always `false`) `commander` bool. Verified against the real WvW fixture: local marker id 3201 resolves to GUID `1993fadb6fb70e4383a223a54d311f7d`, exactly GW2EI's `PurpleCommanderTag`. EI JSON can't express this — native-only. |
| 6 | Eye candy: `CBTS_TRANSFORMATION` + `CBTS_GLIDER` can be used for mounts and glider respectively. | Backlog — cosmetic; slot into the combat-replay/HTML-report milestone where mounted/gliding state actually renders. TRANSFORMATION GUIDs resolve via IDTOGUID (type 6). |
| 7 | `CBTS_TICK` would be nice in a corner to show tick rate. | Done (data) — M2 Task 7. `CBTS_TICK` = sc 84 (hand-counted against the arcdps README + cross-checked against GW2EI's `ArcDPSEnums.StateChange.Tick = 84`). Payload: `src_agent` = current extrapolated tick counter, `dst_agent` = ticks since last real tick update (per the arcdps README; `dst_agent`'s exact semantics aren't independently corroborated anywhere in GW2EI, so `wvw::markers::resolve_tick_rate` deliberately derives ticks/sec from the unambiguous field instead — the `src_agent` counter delta between consecutive events divided by real elapsed time — rather than build on an unverified interpretation). Native schema: `encounter.tick_rate { avg, min, per_second[] }` (tick dips = objective skill-lag signal in large WvW fights), omitted when the log has fewer than two `CBTS_TICK` events — true of the current golden fixture, so this is synthetic-test-verified only pending a fixture that has the event. Corner-widget display deferred to the HTML-report milestone. |
| 8 | Eye candy: `CBTS_GADGETCAPTURE*` for animating objective capping (goes with the zone/team colours already implemented). | Backlog — combat-replay/HTML milestone (animate cap progress on the map). |
| 9 | `CBTS_MISSILELAUNCH`/`CBTS_MISSILEREMOVE` show how effectively reflects/projectile destruction are placed; likely "massive visual pasta" — should be opt-in. | Split: analytics (per-player projectiles destroyed/reflected counts — strong WvW metric) planned for the M3/M4 stats era behind an opt-in flag (data volume); visualization backlog for replay/HTML, also opt-in. |
| 10 | M2 follow-up: a post-May-2026 arcdps capture is needed to re-calibrate the `is_cc` CC-application predicate (post-`ResultEnumRework` builds, `>= 20260501`, can carry genuine CC under `buff == 1`, unlike the pre-rework byte layout `is_cc` was originally tuned against). | Partially addressed — M4 Task 2. `is_cc` (`analysis/cc.rs`) is now era-gated off `RawHeader::is_post_buff_rework`: `buff == 1` rows are accepted as CC post-era, rejected pre-era — cross-checked against GW2EI's `CombatEventFactory.AddBuffDamageDamageEvent`, which routes the shared `DamageResult` enum (incl. `CrowdControl = 12`) through post-era instead of the retired pre-era `ConditionResult` enum. Locked in by synthetic era-equivalence tests, not yet a real capture — the "post-May-2026 capture" this follow-up asked for still doesn't exist, so the post-era branch is verified by construction (GW2EI source + tests) only. `crates/axilog-core/tests/postrework_golden.rs` (M4 Task 3) is the hook that will exercise it against real numbers the moment one is dropped at `fixtures/local/wvw-postrework.zevtc` — see README "Supported log eras". The other two M2 follow-ups this same note bundled (WVWTEAMS/IDTOGUID/TICK real-log paths) remain open; unaffected by this task. |

## Notes

- These features are part of axilog's differentiation: arcdps-spec data that EI's JSON does not
  (or only partially) surfaces. Native schema exposes them first-class; the EI adapter emits only
  what EI's shape supports.
- Verify every enum value and payload layout against the arcdps EVTC reference
  (https://www.deltaconnected.com/arcdps/evtc/README.txt) at implementation time — do not trust
  memory or third-party writeups.
