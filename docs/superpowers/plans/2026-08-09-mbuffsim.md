# axilog MBUFFSIM — Buff-simulator fidelity: Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development.

**Goal:** implement GW2EI's missing stacking logics (ForceOverride, Healing/Regeneration,
StackingConditionalLoss eviction) with an isolate-first discipline, promoting M16's bounded
modifier ids to exact and (if the evidence allows) shrinking the M3 Stability allowlist —
without moving a single already-exact calibrated cell.

## Global Constraints

- GW2EI (/tmp/gw2ei) NoID simulator family is the arbiter (`UseBuffInstanceSimulator=false`
  verified at CombatData.cs:611). Cite every rule.
- HARD INVARIANT: every currently-EXACT calibrated cell stays exact (M3 presence 444/444,
  generation 148/148, avg-stack 67/74 non-allowlisted; M16's 30 exact ids; every other suite).
  Changes may only improve allowlisted/bounded cells. A regression in an exact cell = defect,
  revert. Committed-fixture boon numbers are always-on: any diff there must be an improvement
  justified cell-by-cell vs the EI golden.
- Isolate-first: no implementation before the per-class divergence is reproduced and the
  divergent rule identified (esp. d312/d369 — mechanism explicitly NOT isolated; the naive
  "doesn't refresh" theory is already disproven).
- MATTRIB's incoming-deficit account must not be claimed fixed here (structural allowlist
  stays).
- All suites green with/without AXILOG_LOCAL_FIXTURES; clippy baseline; determinism; no PII.
- MIT.

---

### Task 1: Isolation — reproduce and identify each divergent rule

**Files:** instrumentation only (throwaway or #[ignore] tests + a report); NO simulator
changes.

**Requirements:** for each target class (Might-25 saturation d422; Stability eviction
d-425/426/427/428; Force-type d312/d369; distinct-boons damageGain d174/d111): reconstruct the
per-buff stack timeline axilog computes vs what EI's modifier numbers imply (the M16 golden's
per-row deltas are the oracle); read the corresponding GW2EI logic
(QueueLogic/HealingLogic/ForceOverrideLogic/StackingConditionalLoss + capacity/type defaults
per buff id) and name the EXACT divergent rule per class, with the C# citation and a
reproduction (synthetic event sequence where axilog and the GW2EI rule produce different
stacks). Deliverable: a written rule-by-rule diagnosis in the report + synthetic repro tests
(#[ignore] or plain, asserting the CURRENT wrong behavior with a "MBUFFSIM Task 2 flips this"
note). If a class turns out NOT to be simulator-side after all (e.g. d312/d369), say so and
route it (MATTRIB? engine?) — do not force it.

### Task 2: Implement the diagnosed rules

**Files:** `crates/axilog-core/src/analysis/buffs/simulator.rs` (+ the per-buff stack-type
table — extend M16's catalog::buff_stack or the buffs module's own classification); flip the
Task 1 repro tests.

**Requirements:** implement exactly the rules Task 1 diagnosed — per-buff dispatch, no
speculative extras. Every Task 1 repro test flips to asserting the correct behavior. The M3
suites and every other calibrated suite stay green UNCHANGED except allowlisted/bounded cells,
which may improve. Committed fixture: if boon cells move, document each cell's before/after vs
the EI golden (improvement required). Unit tests per new logic (Force replace incl. the
IsFull==count==1 semantics; Regeneration; eviction order).

### Task 3: Recalibration sweep + promotion + docs

**Files:** M16's ID_BOUNDS (damage_mods_golden.rs) re-seeded; M3 boons_golden allowlist
re-measured; README accuracy notes; report.

**Requirements:** re-run the full post-era calibration; promote every modifier id that went
exact to IdBound::exact (count = the milestone's headline); re-measure the M3 Stability
allowlist (shrink if cells now pass — with before/after per cell); re-seed remaining bounds
from the new measurements (tighter). Verify support/cleanse and every other buff consumer
unaffected (they read events, not simulation — confirm). README: update the boon/modifier
accuracy statements. GATES: full workspace + node + py green; all exact cells provably
unmoved; warning-free.

## Self-Review
Isolate-first protects the calibrated surface; the hard invariant is explicit; d312/d369's
unknown mechanism is routed honestly rather than assumed. No placeholders.
