# axilog MATTRIB — Orphaned-instid repair: Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development.

**Goal:** one shared, GW2EI-exact orphaned-instid address repair replacing M16's module-local
workaround, with every moved calibrated value justified against EI, and the M16 deficit
hypothesis settled.

## Global Constraints

- GW2EI `EvtcParser.CompleteAgents` (/tmp/gw2ei, `EvtcParser.cs:~1130-1250`) is the arbiter —
  transcribe the ACTUAL algorithm (eligibility, the ±300ms aware-window bound, candidate
  selection, src vs dst handling) with citations; do not reuse M16's NonZeroAddrIndex semantics
  unverified (its unbounded lookback was flagged as divergent).
- Moved-output discipline: every changed value on the committed fixture (all formats) and every
  golden-suite delta on the local export must be enumerated and justified closer-or-equal vs
  EI. A previously-exact value moving AWAY from EI = defect.
- All suites green with/without AXILOG_LOCAL_FIXTURES; clippy warning-line baseline; node+py on
  ripple; determinism; no PII. MIT.

---

### Task 1: The repair pre-pass + GW2EI transcription

**Files:** `crates/axilog-core/src/evtc/` or `model/` (decide: GW2EI repairs between agent
table and analysis — place it so ALL consumers see repaired addrs, incl. replay/missiles/
health standalone builders); unit tests (synthetic orphaned rows: eligible, out-of-window,
instid-reuse, src-side vs dst-side).

**Requirements:** transcribe `CompleteAgents`' orphaned-instid path exactly (cite every rule);
implement as a pre-pass producing repaired events (in-place rewrite mirroring GW2EI vs a
repaired copy — pick the perf-sane option; MPERF's bench exists, measure). Retire
`damage_mods::NonZeroAddrIndex` in favor of the shared repair — the M16 modifier goldens must
come out unchanged-or-improved (this is the first calibration proof). Report which surfaces'
raw event streams changed and by how many rows (both fixtures).

### Task 2: Recalibration + deficit hypothesis + docs

**Files:** golden suites (expectations that legitimately move), README accuracy notes, report.

**Requirements:** full-format committed-fixture diff vs pre-MATTRIB main — enumerate every
changed value with its EI-golden justification (closer-or-equal); local export: all suites
before/after per-suite. Deficit hypothesis: if the M16 deficit account's rows go exact, remove
`INCOMING_DEFICIT_ACCOUNTS` + claim with mechanism; else document and keep. Hard gates stay
hard. README: attribution note (axilog now repairs orphaned-instid rows like EI). GATES: full
workspace + node + py green; clippy baseline; determinism; no PII.

## Self-Review
Two tasks; the repair is a cited transcription placed where every consumer benefits; the
moved-output discipline is explicit; the M16 workaround is retired with proof; the deficit
hypothesis is settled evidence-first. No placeholders.
