# axilog MEIGAP — ei-json adapter gap closure: Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development.

**Goal:** close every ei-json field axibridge reads that is currently blank under axilog (the
30-field audit at axibridge:docs/axilog-cutover-report.md), calibrated vs the local EI export,
so axibridge can flip its parser default to axilog. GW2EI (/tmp/gw2ei) is the arbiter for every
field's semantics; cite everything. The audit table is the authoritative field list — read it
first (READ-only: /var/home/mstephens/Documents/GitHub/axibridge/docs/axilog-cutover-report.md).

## Global Constraints

- Calibration: every new field EXACT vs fixtures/local/wvw-postrework.ei.json for joined
  accounts where EI emits it (documented tolerance only with a cited GW2EI-mechanics reason,
  M3-boon-precision-class); committed fixture golden extended via the established route; ALL
  existing calibration untouched (byte-identity on surfaces this doesn't add to).
- New always-on native passes must be cheap (MPERF bench: no >5% pipeline regression without a
  documented reason); heavy blocks ride existing flags (per-target/1S families → --timeseries /
  --skill-damage / --modifiers as semantically fitting; decide per family with size numbers,
  precedent M12; the ei-json side may force-enable internally the way axibridge's config does).
- ei_float/ei_damage_gain text discipline for every new float; determinism; no PII; suites
  green with/without AXILOG_LOCAL_FIXTURES; clippy warning-line baseline; node/py green on
  ripple; .pyi + index.d.ts + types.d.ts synced for any new native schema block (M14 lesson).
- MIT.

---

### Task 1: Pure mappings + small event passes (native data mostly exists)

**Files:** `crates/axilog-ei/src/lib.rs` (+tests), small additions in `crates/axilog-core`
(incoming-CC/strips pass; per-target downs/kills split), schema if a native block is warranted.

**Requirements:** (a) `selfBuffs`/`groupBuffs`/`squadBuffs` arrays from M3's native generation
(self/group/squad %) — verify GW2EI's exact shape (`[].buffData[0].generation`/`.wasted`; how
wasted is defined — cite; if wasted needs simulation data axilog lacks, emit generation exactly
and wasted per a documented rule); (b) `buffUptimes[].states` (+`statesPerSource` if
axibridge reads it on players — check the audit) from the boon simulation's existing stack
timeline (GW2EI states = [time, stack] transition pairs — verify + calibrate); (c)
`defenses[0].receivedCrowdControl`/`.receivedCrowdControlDuration` (incoming CC — reuse the
M2/M13-verified CC predicate, incoming direction) and `.boonStrips`/`.boonStripsTime` (incoming
strips — the support pass's removal machinery, incoming); (d) `statsTargets[i][0]`
`killed`/`downed`/`downContribution`/`damage`/`connectedHits`/`againstDownedCount` — per-target
splits of existing passes (damage per-target exists in skill_damage.per_target; downs/kills
per-target from the downs pass keyed by target; downContribution per-target from M11's
contribution family per-target attribution — verify what GW2EI's statsTargets downContribution
means and whether M11's data supports it; hit-quality per-target from hit_stats keyed by
target). Calibrate each EXACT vs the local export.

### Task 2: Series splits + targets[] mirrors

**Files:** `crates/axilog-core/src/analysis/timeseries.rs` (power-split series),
`skill_damage.rs`/new (per-enemy incoming dist), `crates/axilog-ei` mappings, tests.

**Requirements:** (a) `powerDamageTaken1S` + `targetPowerDamage1S` (power = strike+life-leech
per the established buckets; cumulative 1S grid identical to damage1S); (b) `targets[].damage1S`
+ `targets[].powerDamage1S` (per-enemy incoming series — the timeseries pass keyed by target);
(c) `targets[].totalDamageDist` (per-enemy incoming skill distribution — the skill_damage
machinery, enemy-keyed; verify GW2EI's targets[] dist semantics incl. the hits definition —
the M12 hits-divergence note applies, decide + document); (d) `targets[].buffs[].id` +
`.statesPerSource` (incoming-conditions attribution on enemy players — condition uptime states
per source; scope to what axibridge reads — verify in the audit what its reader consumes and
emit exactly that faithfully; GW2EI targets[].buffs semantics cited). Gate heavy families on
the fitting existing flags; measure sizes. Calibrate EXACT vs local export (the targets join
uses agent identity per the M16 pattern — EI exports 57 curated targets, axilog 624: only
joined targets are calibratable; assert those).

### Task 3: Remainder + calibration sweep + release prep

**Files:** healing dist/series (`extHealingStats.outgoingHealingAllies`/`.totalHealingDist`/
`.healing1S`, extBarrierStats equivalents) from the healing-extension events; `minions[]`
(minion damage-taken rollups — scope to audit-read fields); `guildID` (verify what GW2EI
derives it from — if it needs GW2-API/external data axilog lacks, document as the ONE remaining
gap with axibridge sign-off note instead of faking); full calibration sweep (all suites, both
eras); README parity table updates; docs/BENCHMARKS size notes; the audit-closure summary in
the report (30 fields → N closed / M remaining with reasons — the deliverable axibridge's
re-audit consumes).

## Self-Review
Field list is externally fixed (the audit); every family calibrated against real EI output;
heavy families gated; the one potentially-unclosable field (guildID) is routed honestly. No
placeholders.
